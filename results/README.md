# Results

Versioned result tables belong here after every row has a checkpoint identity,
adapter list, context mode, sample count, request-error count, and metric
normalization. Released Base and Context SFT results must be separate.
